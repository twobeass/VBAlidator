# VBA010 — Syntax error

**Severity:** 🔴 error    **Category:** `syntax`    **Phase:** 0

## Description

Generic syntax error (unexpected `End X`, missing `Then`, stray block terminator …).

## Failing example

```vb
If x > 0
    Debug.Print x
End If
```

## Compliant example

```vb
If x > 0 Then
    Debug.Print x
End If
```

## How to fix

Check the surrounding tokens — typical causes are missing `Then` or mismatched block keywords.

---

_Source: [src/rules.py](https://github.com/twobeass/VBAlidator/blob/main/src/rules.py) — entry `VBA010`._
