# Examples

Sample inputs / outputs that illustrate workflows but are not part of
the runtime package.

| File | Used for |
|------|----------|
| `vba_references.example.json` | Reference Visio-host export from `tools/VBA_Model_Exporter.bas`. Pipe it through `python tools/generate_model.py examples/vba_references.example.json -o /tmp/visio_model.json` to see how a custom model is built. |
